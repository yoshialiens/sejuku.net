<% if @contact.errors.any? %>
  <div id="error_explanation">
    <div class="alert alert-error">
      <%= @contact.errors.count %>個のエラーがあります。
    </div>
    <ul>
    <% @contact.errors.full_messages.each do |msg| %>
      <li>* <%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>
